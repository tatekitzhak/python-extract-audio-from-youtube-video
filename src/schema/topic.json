{
    "bsonType": "object",
    "title": "Topic Object Validation",
    "required": [
        "title"
    ],
    "properties": {
        "title": {
            "bsonType": "string",
            "description": "'Title' of topic must be a string and its required"
        },
        "description": {
            "bsonType": "string",
            "description": "'Description' of the topic must be a string"
        },
        "video": {
            "bsonType": "object",
            "required": [
                "url"
            ],
            "properties": {
                "url": {
                    "bsonType": "string",
                    "description": "'Video' of the topic must be a string of array"
                }
            }
        },
        "article": {
            "bsonType": "array",
            "description": "'Article' of the topic must be a string of array and ref to article",
            "minItems": 0,
            "items": {
                "bsonType": "string"
            }
        },
        "author": {
            "bsonType": "string",
            "description": "'Author' of the topic must be a string"
        },
        "views": {
            "bsonType": "int",
            "minimum": 0,
            "description": "'Views' of the topic must be a int "
        },
        "rating": {
            "bsonType": "double",
            "minimum": 0.00,
            "maximum": 5.00,
            "description": "'Rating' of the topic must be a double "
        },
        "subscribers": {
            "bsonType": "int",
            "minimum": 0,
            "description": "'Subscribers' of the topic must be a int"
        },
        "tags": {
            "bsonType": "array",
            "description": "'Tags' of the topic must be a string of array",
            "minItems": 0,
            "items": {
                "bsonType": "string"
            }
        },
        "subcategory": {
            "bsonType": "array",
            "description": "'Subcategory' must be a string of array and ref to subcategory",
            "minItems": 0,
            "items": {
                "bsonType": "string"
            }
        },
        "thumbnail": {
            "bsonType": "string",
            "description": "'Thumbnail' must be a string"
        },
        "images": {
            "bsonType": "string",
            "description": "'Image' must be a string"
        }
    },
    "additionalProperties": false
}